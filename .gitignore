# Dependency directories
node_modules/
jspm_packages/

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local environment configuration
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS files
.DS_Store
Thumbs.db

# Agent configurations
.agents/
verify_api.js # Exclude smoke test from production git if preferred, or keep it (I will keep it untracked by default to avoid cluttering, or track if desired)
